---
layout: default
title: Page not found
permalink: /404.html
---

<h1 class="page-title">Page not found</h1>

<p>That URL does not exist. The essay may have moved or the link is wrong.</p>

<p class="more-link"><a href="/">Home</a> · <a href="/essays/">All essays</a></p>

{% assign pool = site.pages | where_exp: "p", "p.description" %}
{% assign picks = pool | sample: 4 %}
{% if picks.size > 0 %}
<aside class="related-posts">
  <h3 class="section-label">Try one of these</h3>
  <ul class="essays-list">
    {% for p in picks %}
      <li>
        <a href="{{ p.url }}">
          <strong>{{ p.title }}</strong>
          <span>{{ p.description }}</span>
        </a>
      </li>
    {% endfor %}
  </ul>
</aside>
{% endif %}
